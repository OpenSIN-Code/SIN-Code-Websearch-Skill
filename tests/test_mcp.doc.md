# `test_mcp.py` — MCP Server Tests

What: Unit tests for the FastMCP server tools.

## Coverage

- websearch_search (success and cached)
- websearch_status
- websearch_cache (get, clear, size, list, unknown action)
- websearch_history
- websearch_rate_limit
- Parameter forwarding (engine, num_results)
